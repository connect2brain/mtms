version: '3.5'

services:
  planner_test:
    networks:
      - cinetwork
    build:
      context: ..
      dockerfile: ros2_ws/src/ui/planner/Dockerfile

networks:
  cinetwork:
    name: cinetwork
    driver: bridge